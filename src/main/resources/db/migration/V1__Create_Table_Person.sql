CREATE TABLE `person` (
                          `id` bigint NOT NULL AUTO_INCREMENT,
                          `first_Name` varchar(75) NOT NULL,
                          `last_NameE` varchar(75) NOT NULL,
                          `address` varchar(80) NOT NULL,
                          `gender` varchar(6) NOT NULL,
                          PRIMARY KEY (`id`)
);
